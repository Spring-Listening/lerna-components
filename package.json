{
  "name": "root",
  "private": true,
  "workspaces":[
    "packages/*"
  ],
  "scripts": {
    "web": "lerna exec --scope example-web -- yarn serve",
    "lerna": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
